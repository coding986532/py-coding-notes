# how to convert different types of values to different things:
first = input("Enter your first digit: ")
second = input("Enter your second digit: ")
sum = float(first) + float(second)
# float keeps the input as a decimal.
print("Sum: " + str(sum))